
/**
 * Get all repositories of informed user
 * @param req
 * @param res
 * @returns void
 */
export function getRepos(req, res) {
    req.params.user;

    // if (err) {
    //     res.status(500).send(err);
    // }
    res.json({});
}